schema_version: 1
project_id: TOKENMETER
name: TokenMeter
purpose: Collect and visualize token-usage metadata from supported AI development tools without reading message content or secrets.
success_metrics:
  - Supported local agents can be collected and normalized without reading prompts, responses, or credentials.
  - The central dashboard reports usage by tool, model, host, profile, and date.
  - Canonical tests and syntax checks pass from the repository root.
current_milestone:
  id: TOKENMETER-002
  title: Verify Codex usage accounting
  status: DONE
health: GREEN
next_action: Monitor the next scheduled upload while active Codex work continues.
updated_at: "2026-07-12T10:56:11+08:00"
validation_commands:
  - "PYTHONPATH=src python3 -m unittest discover -s tests"
  - "PYTHONPATH=src python3 -m py_compile src/tokenmeter/*.py"
  - "sh -n scripts/install.sh"
